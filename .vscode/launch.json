{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "GDB",
            "preLaunchTask": "make",
            "program": "${workspaceFolder}/bin/dbf-cat",
            "cwd": "${workspaceFolder}",
            "args": ["dbf-cat.c"]
        }
    ]
}